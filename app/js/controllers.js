'use strict';

/* Controllers */

var controllers = angular.module('controllers', []);

controllers.controller('SubmitCtrl', ['$scope',
  function($scope) {
    //$scope.features = FeatureService.query();
  
  }

]);